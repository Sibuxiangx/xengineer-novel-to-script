请检查本地分章结果是否可靠。

你会看到：
- 当前分章规则。
- 本地按规则切分后的统计与标题预览。
- 本地用宽松启发式扫描出的疑似标题候选，以及其中未被当前规则匹配的候选。

你的任务：
1. 判断当前分章结果是否可以接受。
2. 如果可以接受，accepted=true，context_requests 为空。
3. 如果不可接受，accepted=false，并请求你重写规则所需的最小文本范围。
4. 请求范围应优先围绕未匹配标题候选或异常章节附近，避免请求整本书。
5. 不要在本步骤直接给出新规则；本步骤只做检查与请求上下文。

